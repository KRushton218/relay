export function CloudThinkingSection() {
    return (
      <section className="py-24">
        <div className="max-w-5xl mx-auto text-center space-y-6">
          <h2 className="text-3xl font-semibold text-brand-text">
            Thoughts Don’t Happen in Lists
          </h2>
          <p className="text-base text-brand-muted">
            Your thinking isn’t linear. It’s cloud-like — clusters of half-formed insights and buried connections.
          </p>
          <p className="text-base text-brand-muted">
            Relay sees the shape of your ideas — not the tools they came from.
          </p>
        </div>
      </section>
    );
  }